# Build stage
FROM node:21

WORKDIR /app

RUN npm install -g serve

COPY package*.json ./

RUN npm install

COPY . .

RUN npm run build

EXPOSE 80

CMD ["serve", "-s", "build", "-l", "tcp://0.0.0.0:80"]
